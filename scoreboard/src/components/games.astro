---
const { games, players } = Astro.props;

function getResult(pgnResult: string, player: string)
{
    if (pgnResult === '1-0') return `${player} won`;
    else if (pgnResult === '0-1') return `${player} lost`;
    else if (pgnResult === '1/2-1/2') return 'Draw';
    else return 'Unknown';
}

---

<section>
    <h2>Games</h2>
    <div class="card">
        <table class="games">
            <thead>
                <tr>
                    <th>Timestamp</th>
                    <th>Players</th>
                    <th>Result</th>
                    <th align="center">Data</th>
                </tr>
            </thead>
            <tbody>
                {
                    games.map((g, i) => (
                        <tr>
                            <td>
                                {g.timestamp}
                            </td>
                            <td>
                                <div class="player"><span class={`dot white`}></span>{players[g.white]}</div>
                                <div class="player"><span class={`dot black`}></span>{players[g.black]}</div>
                            </td>
                            <td>{getResult(g.result, players[g.white])}</td>
                            <td align="center">
                                <button
                                    class="btn copy"
                                    data-pgn={g.pgn}
                                    onclick="copyPGN(this)"
                                >
                                    PGN
                                </button>
                            </td>
                        </tr>
                    ))
                }
            </tbody>
        </table>
    </div>
</section>